# Base route URL for API
VITE_API_BASE_URL=http://localhost:3000/api/v1

# VAPID public key for Web Push notifications (must match the backend NOTIFICATION_WEB_PUSH_PUBLIC_KEY)
VITE_WEBPUSH_PUBLIC_KEY=